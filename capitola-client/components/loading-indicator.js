import Layout from "./layout";
import { Typography } from "@mui/material";

export default function LoadingIndicator() {
  return (
    <Layout>
      <Typography variant="h2" mt="40px">Loading..</Typography>
    </Layout>
  );
}
